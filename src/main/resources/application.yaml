spring:
  application:
    name: store

stripe:
  apiUrl: https://api.stripe.com
  enabled: true
  timeout: 1000
  supported-currencies: USD,EUR,INR

logging:
  level:
    org:
      springframework:
        boot:
          devtools: DEBUG

payment-gateway: paypal